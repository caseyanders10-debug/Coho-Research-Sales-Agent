name: Hotel Intelligence Agent

on:
  workflow_dispatch:
    inputs:
      email_body:
        description: 'Paste the raw ZoomInfo email body (or a single hotel name)'
        required: true
        type: string

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          python -m playwright install --with-deps chromium

      - name: Ensure screenshots directory
        run: mkdir -p screenshots

      - name: Run Agent
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          EMAIL_INPUT: ${{ github.event.inputs.email_body }}
        run: python -u hotel_agent_script.py

      - name: Debug - list workspace and screenshots
        if: always()
        run: |
          echo "PWD: $(pwd)"
          echo "Repo files:"
          ls -la
          echo "Screenshots dir:"
          ls -la screenshots || true
          echo "Find generated files:"
          find . -maxdepth 3 -type f \( -name "*.xlsx" -o -name "*.json" -o -name "*.txt" -o -name "*.png" -o -name "*.html" \) -print

      - name: Upload Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: hotel-output
          path: screenshots/
          if-no-files-found: warn

